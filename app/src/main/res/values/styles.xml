<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
   <style name="primaryBtn">
       <item name="android:background">@drawable/round_button</item>
        <item name="android:textColor">@color/colorPrimaryBtnText</item>
   </style>
    <style name="secondaryBtn">

        <item name="android:background">@drawable/button_border</item>
        <item name="android:textColor">@color/colorSecondaryBtnText</item>
        
    </style>
    <style name="secondaryRoundedButton">
        <item name="android:background">@drawable/round_border_button</item>
        <item name="android:textColor">@color/colorSecondaryBtnText</item>
    </style>
    <style name="clickableText">
        <item name="android:textColor">@color/colorRegisterText</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
