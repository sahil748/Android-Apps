<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MoreDetailsDialog" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/secondaryText</item>
        <item name="android:textColorPrimary">@color/moreDetailsDbPrimaryText</item>
    </style>
    <style name="RoundedButton">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_width">@dimen/button_width</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/rounded_border_button</item>
        <item name="android:textColor">@color/secondaryText</item>
    </style>

</resources>
